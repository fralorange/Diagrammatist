<Window x:Class="Diagrammatist.Presentation.WPF.Views.Dialogs.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diagrammatist.Presentation.WPF.Views.Dialogs"
        xmlns:vmd="clr-namespace:Diagrammatist.Presentation.WPF.ViewModels.Dialogs"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:schemes="clr-namespace:Diagrammatist.Presentation.WPF.Core.Models.ColorScheme;assembly=Diagrammatist.Presentation.WPF.Core"
        xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
        lex:ResxLocalizationProvider.DefaultDictionary="Resources.Localization.Dialogs.Settings.SettingsResources"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance {x:Type vmd:SettingsDialogViewModel}}">
    <Window.Resources>
        <Style TargetType="Border">
            <Setter Property="Background" Value="{StaticResource FrameBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource FrameStroke}" />
            <Setter Property="BorderThickness" Value="0.5" />
        </Style>
    </Window.Resources>
    <Grid Width="650"
          Height="750">
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TabControl Grid.Row="0" 
                    TabStripPlacement="Left"
                    Background="{StaticResource SettingsTabsBackground}">
            <TabItem Header="{lex:Loc System}">
                <Border>
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" MinWidth="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" 
                                   Grid.Column="0" 
                                   Text="{lex:Loc Languages}"/>
                        
                        <ComboBox Grid.Row="0" 
                                  Grid.Column="1"
                                  ItemsSource="{Binding AvailableLanguages}"
                                  SelectedItem="{Binding SelectedLanguage}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NativeName}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                </Border>
            </TabItem>
            <TabItem Header="{lex:Loc Schemes}">
                <Border>
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ListView Grid.Column="0" 
                                  Margin="0,0,5,0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="{lex:Loc ColorScheme}" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" Margin="5"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn x:Name="column"
                                                    Header="{lex:Loc ColorValue}" 
                                                    Width="120">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <colorpicker:PortableColorPicker Style="{StaticResource PortableColorSlider}"
                                                                                 SelectedColor="{Binding Hex, Converter={StaticResource HexToMediaColorConverter}}"
                                                                                 MinWidth="30"
                                                                                 MinHeight="30"
                                                                                 ShowAlpha="True"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>

                            <ListView.Items>
                                <schemes:ColorSchemeEntry Name="Main Theme" Hex="#FF9370DB"/>
                                <schemes:ColorSchemeEntry Name="Background" Hex="#FFFFFFFF"/>
                                <schemes:ColorSchemeEntry Name="Text Color" Hex="#FF000000"/>
                            </ListView.Items>

                            <ListView.ItemContainerStyle>
                                <Style TargetType="{x:Type ListViewItem}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>

                        <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top">
                            <Button Content="{lex:Loc Import}" Margin="0,0,0,5" Padding="10,3"/>
                            <Button Content="{lex:Loc Export}" Padding="10,3"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Grid.Row="1">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="5" />
                    <Setter Property="Padding" Value="5" />
                </Style>
            </StackPanel.Resources>

            <Button Content="{lex:Loc Diagrammatist.Presentation.WPF:Resources.Localization.Dialogs.AddCanvas.AddCanvasResources:OK}"
                    Command="{Binding OkCommand}"
                    IsDefault="True"/>

            <Button Content="{lex:Loc Diagrammatist.Presentation.WPF:Resources.Localization.Dialogs.AddCanvas.AddCanvasResources:Cancel}"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
